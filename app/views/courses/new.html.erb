<h1> Add Course </h1>
  <%= form_with(model: @course, local: true, class: "ui form") do |f|%>
    <div class = "field">
      <%= f.text_field :name, autofocus: true, placeholder: "Course Name", required: true %>
      </div>
      <%= f.submit class: "ui green button" %>
    <% end %>

